# API token for the configured image generation provider (required)
IMAGE_TOOL_API_KEY=your-api-key

# Optional: override the default host (defaults to https://api.cometapi.com)
# IMAGE_TOOL_API_HOST=https://images.example.com
