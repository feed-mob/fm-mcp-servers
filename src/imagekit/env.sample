# API token for the configured image generation provider (required)
IMAGE_TOOL_API_KEY=your-api-key

# Optional: override the default base URL (defaults to https://api.cometapi.com/v1)
# IMAGE_TOOL_BASE_URL=https://api.other-provider.com/v1

# Optional: override the model identifier (defaults to bytedance-seedream-4-0-250828)
# IMAGE_TOOL_MODEL_ID=different-model-id
