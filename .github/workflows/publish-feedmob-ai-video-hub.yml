name: Publish FeedMob AI Video Hub Package

on:
  push:
    branches:
      - main
    paths:
      - 'src/feedmob-ai-video-hub/**'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Confirm publishing AI Video Hub package'
        required: true
        type: boolean
        default: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    if: ${{ github.event_name == 'push' || github.event.inputs.publish == 'true' }}
    defaults:
      run:
        working-directory: src/feedmob-ai-video-hub

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
